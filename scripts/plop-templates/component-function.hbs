import React from 'react'
import PropTypes from 'prop-types'

import { Root } from '~/{{directory}}/{{properCase name}}/{{properCase name}}{{properCase shardSuffix}}.shards'

const {{properCase name}}{{properCase nameSuffix}} = () => (
  <Root>
    This is the {{properCase name}} component!
  </Root>
)

{{properCase name}}{{properCase nameSuffix}}.propTypes = {
  usePropTypes: PropTypes.object.isRequired
}

export default {{properCase name}}{{properCase nameSuffix}}
