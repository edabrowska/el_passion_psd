/* eslint-env jest */

import React from 'react'
import renderer from 'react-test-renderer'
import serializer from 'jest-emotion'

import {{properCase name}}{{properCase nameSuffix}} from '~/{{directory}}/{{properCase name}}/{{properCase name}}{{properCase nameSuffix}}'

expect.addSnapshotSerializer(serializer)

describe('{{properCase name}}{{properCase nameSuffix}}', () => {
  {{#if skipSnapshot}}
  // test cases here
  {{else}}
  it('Snapshot', () => {
    const component = renderer.create(<{{properCase name}}{{properCase nameSuffix}} />)
    const tree = component.toJSON()
    expect(tree).toMatchSnapshot()
  })
  {{/if}}
})
