/* eslint-env jest */

import { mount } from 'enzyme'
import React from 'react'
import renderer from 'react-test-renderer'

import {{properCase name}} from '~/components/{{properCase name}}'

describe('{{properCase name}}', () => {
  it('Renders correctly', () => {
    const app = mount(<{{properCase name}} />)

    expect(app.find('.{{kebabCase name}}').text()).toEqual('This is the {{properCase name}} component!')
  })

  it('Snapshot', () => {
    const component = renderer.create(<{{properCase name}} />)
    const tree = component.toJSON()
    expect(tree).toMatchSnapshot()
  })
})
